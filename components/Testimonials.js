'use client'

/**
 * Renders testimonials when `testimonials` is exported from app/data.js.
 * Section is omitted when the dataset is unavailable.
 */
export default function Testimonials() {
  return null
}
